{
  "name": "driftly-source",
  "version": "1.0.0",
  "description": "Email Automation Platform",
  "scripts": {
    "start": "cd driftly-frontend && npm start",
    "start:backend": "cd driftly-backend && npm start",
    "dev": "concurrently \"npm run start\" \"npm run start:backend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
